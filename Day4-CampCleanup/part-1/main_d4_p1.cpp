#include <bits/stdc++.h>

#define forn(i, n) for (int i = 0; i < n; ++i)
#define fore(i, l, r) for (int i = l; i < r; ++i)

#define scasti(x) static_cast<int>(x)

using namespace std;

using ll = long long;

void solve()
{
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
}

